# 도감(Dogam) 조회 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `GET /api/me/dogam/overview·regions·recent` — 사용자 수집(visit) 집계로 전국 현황·시·도별 카드·최근 수집 목록 제공.

**Architecture:** 신규 `dogam` 모듈. 자체 repository의 GROUP BY 집계로 N+1 회피. 시·도 이름·정렬은 기존 `RegionsService.listRegions` 재사용(RegionsModule에 export 추가). recent는 기존 cursor 인프라 + 최신 내 인증사진 조인. 신규 테이블 없음.

**Tech Stack:** NestJS 11, Drizzle(PostgreSQL+PostGIS), Jest. 스펙: `docs/superpowers/specs/2026-07-11-dogam-design.md`

## Global Constraints

- **브랜치**: `feat/dogam` (main 최신에서 생성). Co-Authored-By 트레일러 금지.
- **툴체인**: `export PATH="$HOME/.nvm/versions/node/v24.14.0/bin:$HOME/Library/pnpm:$PATH"` 후 `corepack pnpm ...`. `lint` 실행 금지.
- **응답 envelope**: 성공 `{result:...}`만 — 컨트롤러는 payload만 반환.
- **정책(정확값)**:
  - "수집" = 해당 ACTIVE place에 대한 `visit` 존재.
  - overview: `total`=전국 ACTIVE place, `collected`=내 방문 distinct ACTIVE place, `percent`=`total>0?round(collected/total*100):0`.
  - regions: 시·도 **17개 전부**(방문 0 포함), `Number(sidoCode)` 오름차순, `name`=locale/KO 폴백, province별 집계는 `region.parent_code` GROUP BY, `locked`=**항상 false**.
  - recent: visit `createdAt DESC, id DESC` 커서(limit 기본 20, max 100), `imageUrl`=그 (user,place)의 최신 **ACCEPTED** certification.image_key → `/api/certifications/photos/{image_key}`, 없으면 null, `collectedAt`=visit.createdAt.
- **경로 별칭**: `@db/schema`, `@platform/...`, `@modules/...`.
- **재사용**: `RegionsService.listRegions(locale): Promise<{code:string,name:string}[]>`(정렬됨), cursor 인프라 `buildCursorPage`/`decodeCursor`/`encodeCursor`(`@platform/pagination/cursor`), `@ReqContext`→`RequestContext.locale`, `JwtAuthGuard`+`@CurrentUser`(AuthModule).
- 현재 테스트 기준선 96. 착수 전 `corepack pnpm test`로 재확인.

---

### Task 1: 도감 overview + regions (repo 집계 + service + module, TDD)

**Files:**
- Modify: `src/modules/regions/regions.module.ts` (RegionsService export 추가)
- Create: `src/modules/dogam/dogam.repository.ts`
- Create: `src/modules/dogam/dogam.service.ts`
- Create: `src/modules/dogam/dogam.module.ts`
- Test: `src/modules/dogam/dogam.service.spec.ts`

**Interfaces:**
- Consumes: `RegionsService.listRegions(locale)`(RegionsModule export), `@db/schema` `places`/`visits`/`regions`.
- Produces:
```ts
// repository
overview(userId): Promise<{ collected: number; total: number }>
regionTotals(): Promise<Map<string, number>>          // province code → ACTIVE place 수
regionVisited(userId): Promise<Map<string, number>>   // province code → 내 방문 distinct
// service
DogamService.overview(userId): Promise<{ percent; collected; total }>
DogamService.regions(userId, locale): Promise<RegionCard[]>
// RegionCard = { sidoCode:string; name:string; percent:number; collected:number; total:number; locked:boolean }
```
Task 2가 repo/service에 recent 추가, Task 3 컨트롤러가 overview/regions 호출.

- [ ] **Step 1: 브랜치 + RegionsService export**

```bash
git checkout main && git checkout -b feat/dogam
```
`src/modules/regions/regions.module.ts`의 `@Module({...})`에 `exports: [RegionsService],` 추가(providers 배열 아래).

- [ ] **Step 2: 실패하는 서비스 테스트 작성**

`src/modules/dogam/dogam.service.spec.ts`:
```ts
import { DogamService } from './dogam.service';

describe('DogamService', () => {
  let repo: any, regionsSvc: any, service: DogamService;

  beforeEach(() => {
    repo = {
      overview: jest.fn(),
      regionTotals: jest.fn(),
      regionVisited: jest.fn(),
    };
    regionsSvc = { listRegions: jest.fn() };
    service = new DogamService(repo, regionsSvc);
  });

  describe('overview', () => {
    it('computes percent from collected/total (rounded)', async () => {
      repo.overview.mockResolvedValue({ collected: 45, total: 370 });
      expect(await service.overview('u1')).toEqual({ percent: 12, collected: 45, total: 370 });
    });
    it('returns percent 0 when total is 0', async () => {
      repo.overview.mockResolvedValue({ collected: 0, total: 0 });
      expect(await service.overview('u1')).toEqual({ percent: 0, collected: 0, total: 0 });
    });
  });

  describe('regions', () => {
    it('merges province names (sorted) with counts, 0-fills unvisited, locked=false', async () => {
      regionsSvc.listRegions.mockResolvedValue([
        { code: '8', name: '세종특별자치시' },
        { code: '39', name: '제주특별자치도' },
      ]);
      repo.regionTotals.mockResolvedValue(new Map([['8', 5], ['39', 40]]));
      repo.regionVisited.mockResolvedValue(new Map([['39', 2]])); // 세종 미방문
      const out = await service.regions('u1', 'KO');
      expect(regionsSvc.listRegions).toHaveBeenCalledWith('KO');
      expect(out).toEqual([
        { sidoCode: '8', name: '세종특별자치시', collected: 0, total: 5, percent: 0, locked: false },
        { sidoCode: '39', name: '제주특별자치도', collected: 2, total: 40, percent: 5, locked: false },
      ]);
    });
  });
});
```

- [ ] **Step 3: 실패 확인**

Run: `corepack pnpm test -- dogam.service`
Expected: FAIL — `Cannot find module './dogam.service'`

- [ ] **Step 4: Repository 작성**

`src/modules/dogam/dogam.repository.ts`:
```ts
import { Inject, Injectable } from '@nestjs/common';
import { and, eq, sql } from 'drizzle-orm';
import { DRIZZLE, type DrizzleDB } from '@platform/database/drizzle.constants';
import { places, visits, regions } from '@db/schema';

@Injectable()
export class DogamRepository {
  constructor(@Inject(DRIZZLE) private readonly db: DrizzleDB) {}

  /** 전국 ACTIVE place 수 + 내 방문 distinct ACTIVE place 수. */
  async overview(userId: string): Promise<{ collected: number; total: number }> {
    const [{ total }] = await this.db
      .select({ total: sql<number>`count(*)::int` })
      .from(places)
      .where(eq(places.status, 'ACTIVE'));
    const [{ collected }] = await this.db
      .select({ collected: sql<number>`count(distinct ${visits.placeId})::int` })
      .from(visits)
      .innerJoin(places, eq(places.id, visits.placeId))
      .where(and(eq(visits.userId, userId), eq(places.status, 'ACTIVE')));
    return { collected: Number(collected), total: Number(total) };
  }

  /** 시·도(parent_code)별 ACTIVE place 수. */
  async regionTotals(): Promise<Map<string, number>> {
    const rows = await this.db
      .select({ province: regions.parentCode, total: sql<number>`count(*)::int` })
      .from(places)
      .innerJoin(regions, eq(regions.code, places.regionCode))
      .where(eq(places.status, 'ACTIVE'))
      .groupBy(regions.parentCode);
    const m = new Map<string, number>();
    for (const r of rows) if (r.province) m.set(r.province, Number(r.total));
    return m;
  }

  /** 시·도(parent_code)별 내 방문 distinct place 수. */
  async regionVisited(userId: string): Promise<Map<string, number>> {
    const rows = await this.db
      .select({ province: regions.parentCode, collected: sql<number>`count(distinct ${places.id})::int` })
      .from(visits)
      .innerJoin(places, eq(places.id, visits.placeId))
      .innerJoin(regions, eq(regions.code, places.regionCode))
      .where(and(eq(visits.userId, userId), eq(places.status, 'ACTIVE')))
      .groupBy(regions.parentCode);
    const m = new Map<string, number>();
    for (const r of rows) if (r.province) m.set(r.province, Number(r.collected));
    return m;
  }
}
```

- [ ] **Step 5: Service 작성**

`src/modules/dogam/dogam.service.ts`:
```ts
import { Injectable } from '@nestjs/common';
import type { localeEnum } from '@db/schema';
import { RegionsService } from '@modules/regions/regions.service';
import { DogamRepository } from './dogam.repository';

type Locale = (typeof localeEnum.enumValues)[number];

export interface RegionCard {
  sidoCode: string;
  name: string;
  percent: number;
  collected: number;
  total: number;
  locked: boolean;
}

@Injectable()
export class DogamService {
  constructor(
    private readonly repo: DogamRepository,
    private readonly regions: RegionsService,
  ) {}

  async overview(userId: string): Promise<{ percent: number; collected: number; total: number }> {
    const { collected, total } = await this.repo.overview(userId);
    return { percent: total > 0 ? Math.round((collected / total) * 100) : 0, collected, total };
  }

  async regions(userId: string, locale: Locale): Promise<RegionCard[]> {
    const names = await this.regions.listRegions(locale); // 정렬된 [{code,name}]
    const totals = await this.repo.regionTotals();
    const visited = await this.repo.regionVisited(userId);
    return names.map(({ code, name }) => {
      const total = totals.get(code) ?? 0;
      const collected = visited.get(code) ?? 0;
      return {
        sidoCode: code,
        name,
        collected,
        total,
        percent: total > 0 ? Math.round((collected / total) * 100) : 0,
        locked: false,
      };
    });
  }
}
```

- [ ] **Step 6: Module 작성** (컨트롤러는 Task 3)

`src/modules/dogam/dogam.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { AuthModule } from '@modules/auth/auth.module';
import { RegionsModule } from '@modules/regions/regions.module';
import { DogamRepository } from './dogam.repository';
import { DogamService } from './dogam.service';

@Module({
  imports: [AuthModule, RegionsModule], // JwtAuthGuard, RegionsService(listRegions)
  providers: [DogamRepository, DogamService],
  exports: [DogamService],
})
export class DogamModule {}
```

- [ ] **Step 7: GREEN + 전체 + 빌드**

Run: `corepack pnpm test -- dogam.service` → PASS 3/3
Run: `corepack pnpm test && corepack pnpm build` → 전체 통과 + 빌드 성공.

- [ ] **Step 8: 커밋**
```bash
git add src/modules/dogam/ src/modules/regions/regions.module.ts
git commit -m "feat(dogam): overview + regions collection progress (visit aggregation)"
```

---

### Task 2: 최근 수집 recent (repo cursor+조인 + service, TDD)

**Files:**
- Modify: `src/modules/dogam/dogam.repository.ts` (recentVisitsPage, placeNames, certImagesFor 추가)
- Modify: `src/modules/dogam/dogam.service.ts` (recent + pickName 추가)
- Test: `src/modules/dogam/dogam.service.spec.ts` (recent describe 추가)

**Interfaces:**
- Consumes: cursor 인프라, `@db/schema` `placeTrans`/`certifications`.
- Produces:
```ts
// repository
recentVisitsPage(userId, limit, cursor?): Promise<{ id:string; createdAt:Date; placeId:string }[]>  // limit+1
placeNames(placeIds: string[], locales: Locale[]): Promise<{ placeId:string; locale:string; name:string }[]>
certImagesFor(userId, placeIds: string[]): Promise<{ placeId:string; imageKey:string }[]>  // 최신 ACCEPTED per place
// service
DogamService.recent(userId, locale, cursor?, limit?): Promise<CursorPage<RecentItem>>
// RecentItem = { placeId:string; name:string; imageUrl:string|null; collectedAt:string }
```
Task 3 컨트롤러가 recent 호출.

- [ ] **Step 1: 실패하는 recent 테스트 추가**

`src/modules/dogam/dogam.service.spec.ts`의 `beforeEach` repo 모킹에 세 메서드를 추가하고(`recentVisitsPage: jest.fn(), placeNames: jest.fn(), certImagesFor: jest.fn()`), describe를 추가:
```ts
  describe('recent', () => {
    const d1 = new Date('2026-07-11T00:00:02.000Z');
    const d2 = new Date('2026-07-11T00:00:01.000Z');

    it('maps visits to items with cert imageUrl (or null) and collectedAt, builds nextCursor', async () => {
      // limit 1 → repo returns limit+1=2 rows → hasNext true
      repo.recentVisitsPage.mockResolvedValue([
        { id: 'v1', createdAt: d1, placeId: 'p1' },
        { id: 'v2', createdAt: d2, placeId: 'p2' },
      ]);
      repo.placeNames.mockResolvedValue([
        { placeId: 'p1', locale: 'KO', name: '오름' },
      ]);
      repo.certImagesFor.mockResolvedValue([
        { placeId: 'p1', imageKey: 'certifications/a.png' },
      ]);
      const out = await service.recent('u1', 'KO', undefined, 1);
      expect(out.items).toEqual([
        {
          placeId: 'p1',
          name: '오름',
          imageUrl: '/api/certifications/photos/certifications/a.png',
          collectedAt: d1.toISOString(),
        },
      ]);
      expect(out.nextCursor).toEqual(expect.any(String));
      expect(repo.recentVisitsPage).toHaveBeenCalledWith('u1', 1, undefined);
    });

    it('imageUrl is null when place has no cert; name falls back empty; last page has null cursor', async () => {
      repo.recentVisitsPage.mockResolvedValue([{ id: 'v9', createdAt: d1, placeId: 'p9' }]);
      repo.placeNames.mockResolvedValue([]); // 이름 없음 → ''
      repo.certImagesFor.mockResolvedValue([]); // 인증 없음 → null
      const out = await service.recent('u1', 'KO', undefined, 20);
      expect(out.items).toEqual([
        { placeId: 'p9', name: '', imageUrl: null, collectedAt: d1.toISOString() },
      ]);
      expect(out.nextCursor).toBeNull();
    });
  });
```

- [ ] **Step 2: 실패 확인**

Run: `corepack pnpm test -- dogam.service`
Expected: FAIL — `service.recent is not a function`

- [ ] **Step 3: Repository 메서드 추가**

`src/modules/dogam/dogam.repository.ts` — import 확장:
```ts
import { and, desc, eq, inArray, lt, or, sql, type SQL } from 'drizzle-orm';
import { decodeCursor } from '@platform/pagination/cursor';
import { places, visits, regions, placeTrans, certifications, type localeEnum } from '@db/schema';

type Locale = (typeof localeEnum.enumValues)[number];
```
클래스에 메서드 추가:
```ts
  /** 내 visit을 createdAt DESC, id DESC 커서로 limit+1개. */
  async recentVisitsPage(
    userId: string,
    limit: number,
    cursor?: string,
  ): Promise<{ id: string; createdAt: Date; placeId: string }[]> {
    const c = decodeCursor(cursor);
    const conds: SQL[] = [eq(visits.userId, userId)];
    if (c) {
      conds.push(
        or(
          lt(visits.createdAt, c.createdAt),
          and(eq(visits.createdAt, c.createdAt), lt(visits.id, c.id)),
        )!,
      );
    }
    return this.db
      .select({ id: visits.id, createdAt: visits.createdAt, placeId: visits.placeId })
      .from(visits)
      .where(and(...conds))
      .orderBy(desc(visits.createdAt), desc(visits.id))
      .limit(limit + 1);
  }

  async placeNames(
    placeIds: string[],
    locales: Locale[],
  ): Promise<{ placeId: string; locale: string; name: string }[]> {
    if (placeIds.length === 0) return [];
    return this.db
      .select({ placeId: placeTrans.placeId, locale: placeTrans.locale, name: placeTrans.name })
      .from(placeTrans)
      .where(and(inArray(placeTrans.placeId, placeIds), inArray(placeTrans.locale, locales)));
  }

  /** (user,place)별 최신 ACCEPTED 인증의 image_key. */
  async certImagesFor(
    userId: string,
    placeIds: string[],
  ): Promise<{ placeId: string; imageKey: string }[]> {
    if (placeIds.length === 0) return [];
    return this.db
      .selectDistinctOn([certifications.placeId], {
        placeId: certifications.placeId,
        imageKey: certifications.imageKey,
      })
      .from(certifications)
      .where(
        and(
          eq(certifications.userId, userId),
          inArray(certifications.placeId, placeIds),
          eq(certifications.status, 'ACCEPTED'),
        ),
      )
      .orderBy(certifications.placeId, desc(certifications.createdAt));
  }
```

- [ ] **Step 4: Service recent + pickName 추가**

`src/modules/dogam/dogam.service.ts` — import 추가:
```ts
import { buildCursorPage, type CursorPage } from '@platform/pagination/cursor';
```
인터페이스 추가:
```ts
export interface RecentItem {
  placeId: string;
  name: string;
  imageUrl: string | null;
  collectedAt: string;
}
```
메서드 추가:
```ts
  async recent(
    userId: string,
    locale: Locale,
    cursor?: string,
    limit?: number,
  ): Promise<CursorPage<RecentItem>> {
    const lim = Math.min(Math.max(limit ?? 20, 1), 100);
    const rows = await this.repo.recentVisitsPage(userId, lim, cursor);
    const page = buildCursorPage(rows, lim);
    const ids = page.items.map((r) => r.placeId);
    const names = await this.repo.placeNames(ids, [locale, 'KO']);
    const images = await this.repo.certImagesFor(userId, ids);
    const imgMap = new Map(images.map((i) => [i.placeId, i.imageKey]));
    const items: RecentItem[] = page.items.map((r) => {
      const key = imgMap.get(r.placeId);
      return {
        placeId: r.placeId,
        name: this.pickName(names.filter((n) => n.placeId === r.placeId), locale),
        imageUrl: key ? `/api/certifications/photos/${key}` : null,
        collectedAt: r.createdAt.toISOString(),
      };
    });
    return { items, nextCursor: page.nextCursor };
  }

  private pickName(
    names: { locale: string; name: string }[],
    locale: Locale,
  ): string {
    return (
      names.find((n) => n.locale === locale)?.name ??
      names.find((n) => n.locale === 'KO')?.name ??
      ''
    );
  }
```

- [ ] **Step 5: GREEN + 전체 + 빌드**

Run: `corepack pnpm test -- dogam.service` → PASS (기존 3 + recent 2)
Run: `corepack pnpm test && corepack pnpm build` → 전체 통과 + 빌드 성공.

- [ ] **Step 6: 커밋**
```bash
git add src/modules/dogam/
git commit -m "feat(dogam): recent collections (cursor + my cert photo)"
```

---

### Task 3: Controller + 앱 배선 + Swagger

**Files:**
- Create: `src/modules/dogam/dto/dogam.dto.ts`
- Create: `src/modules/dogam/dogam.controller.ts`
- Modify: `src/modules/dogam/dogam.module.ts` (controllers)
- Modify: `src/app.module.ts` (DogamModule 등록)

**Interfaces:**
- Consumes: `DogamService.overview/regions/recent`.
- Produces: `GET /api/me/dogam/overview`, `/regions`, `/recent`.

- [ ] **Step 1: DTO 작성**

`src/modules/dogam/dto/dogam.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

export class RecentQueryDto extends createZodDto(
  z.object({
    cursor: z.string().optional(),
    limit: z.coerce.number().int().min(1).max(100).optional(),
  }),
) {}
```

- [ ] **Step 2: Controller 작성**

`src/modules/dogam/dogam.controller.ts`:
```ts
import { Controller, Get, Query, UseGuards } from '@nestjs/common';
import { ApiBearerAuth, ApiOperation, ApiQuery, ApiTags } from '@nestjs/swagger';
import { JwtAuthGuard } from '@modules/auth/guards/jwt-auth.guard';
import { CurrentUser } from '@modules/auth/decorators/current-user.decorator';
import type { AuthUser } from '@modules/auth/auth.types';
import { ReqContext } from '@platform/context/req-context.decorator';
import type { RequestContext } from '@platform/context/request-context';
import { DogamService } from './dogam.service';
import { RecentQueryDto } from './dto/dogam.dto';

@ApiTags('dogam')
@ApiBearerAuth()
@Controller('me/dogam')
@UseGuards(JwtAuthGuard)
export class DogamController {
  constructor(private readonly dogam: DogamService) {}

  /** 전국 수집현황. */
  @Get('overview')
  @ApiOperation({ summary: '도감 전국 수집현황' })
  overview(@CurrentUser() user: AuthUser) {
    return this.dogam.overview(user.userId);
  }

  /** 시·도별 수집 카드. */
  @Get('regions')
  @ApiOperation({ summary: '도감 시·도별 수집현황' })
  regions(@CurrentUser() user: AuthUser, @ReqContext() ctx: RequestContext) {
    return this.dogam.regions(user.userId, ctx.locale);
  }

  /** 최근 수집 목록 (cursor). */
  @Get('recent')
  @ApiOperation({ summary: '도감 최근 수집 목록' })
  @ApiQuery({ name: 'cursor', required: false, type: String })
  @ApiQuery({ name: 'limit', required: false, type: Number })
  recent(
    @CurrentUser() user: AuthUser,
    @Query() q: RecentQueryDto,
    @ReqContext() ctx: RequestContext,
  ) {
    return this.dogam.recent(user.userId, ctx.locale, q.cursor, q.limit);
  }
}
```

- [ ] **Step 3: Module controllers + app.module 등록**

`src/modules/dogam/dogam.module.ts`: `controllers: [DogamController],` 추가 + import.

`src/app.module.ts`: import `import { DogamModule } from '@modules/dogam/dogam.module';` + imports 배열에 `DogamModule,` 추가(CertificationsModule 아래 또는 도메인 모듈 그룹 끝).

- [ ] **Step 4: 전체 + 빌드**

Run: `corepack pnpm test && corepack pnpm build`
Expected: 전체 통과 + 빌드 성공.

- [ ] **Step 5: 커밋**
```bash
git add src/modules/dogam/ src/app.module.ts
git commit -m "feat(dogam): me/dogam endpoints (overview/regions/recent)"
```

---

## 배포/검증 (전체 구현 후)

마이그레이션 없음(조회만) — 배포는 이미지 재빌드만. 앱 구동 후 회원 토큰으로:
1. `GET /me/dogam/overview` → `{percent,collected,total}` (방문 있으면 collected>0).
2. `GET /me/dogam/regions` → 17개, 방문한 도만 collected>0, 정수 코드순, locked 전부 false.
3. `GET /me/dogam/recent` → 최근 방문 DESC, 인증한 곳은 imageUrl 채워짐/아니면 null, nextCursor 페이지네이션.
4. `Accept-Language: EN` → 이름 영문(있으면)/KO 폴백.

## Self-Review 결과

- **스펙 커버리지:** overview→T1, regions(17개·정수정렬·locale·locked)→T1, recent(cursor·내 인증사진·collectedAt)→T2, 3개 라우트/가드/Swagger→T3. "수집=visit" 전제는 repo 쿼리(visit⨝place ACTIVE)에 반영. 누락 없음.
- **Placeholder:** 없음 — 모든 스텝 실제 코드/명령/기대값.
- **타입 일관성:** `RegionCard`/`RecentItem` 필드 ↔ 서비스 반환 ↔ 테스트 기대. `RegionsService.listRegions(locale)→{code,name}[]` ↔ 서비스 사용부. cursor `buildCursorPage`는 `{createdAt,id}` 필요 — `recentVisitsPage` 반환에 둘 다 포함. repo Map 반환 ↔ 서비스 `.get(code)??0`.
- **주의(구현 시):** `selectDistinctOn`는 Drizzle 지원 API — 빌드 실패 시 `sql` 서브쿼리로 대체하되 "(user,place) 최신 ACCEPTED image_key" 시맨틱 유지.
